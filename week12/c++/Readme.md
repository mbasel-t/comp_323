### Readme - Game Dev - OOP Examples - Shooter Arena

Basic game dev example with C++ and SFML.

To build game demo,

* in root of project directory
	* create `build` directory
	* `cd` to `build` directory
	* issue command `cmake ..`
	* issue command `make`
	* start game `./STGArenaSpire`

To start game demo after opening game window,

* press **Enter** key
* then press **1** key
* mouse controls rotation of player sprite

#### Game outline

* v0.2
	* sprite sheet usage
	* background image drawn
	* add randomly generated scrolling background
	* player rotates and moves on x and y
	* ...
